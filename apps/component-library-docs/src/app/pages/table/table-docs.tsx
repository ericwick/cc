import { Table } from '@cc/component-library';

export default function TableDocs() {
  return (
    <div>
      <h1>Tables</h1>
      <p>
        The table component allows the user to show and hide sections of related
        content on a page
      </p>
      <Table />
    </div>
  );
}
