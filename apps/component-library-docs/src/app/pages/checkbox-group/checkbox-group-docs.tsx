import { CheckboxGroup } from '@cc/component-library';

export default function CheckboxGroupDocs() {
  return (
    <div>
      <h1>CheckboxGroups</h1>
      <p>
        The CheckboxGroup component allows the user to show and hide sections of
        related content on a page
      </p>
      <CheckboxGroup />
    </div>
  );
}
